export const metadata = {
  title: "Drops",
  description: "Limited releases, sealed in time. Early access for Sanctuary members.",
};

export default function DropsLayout({ children }) {
  return children;
}
