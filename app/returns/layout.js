export const metadata = {
  alternates: { canonical: 'https://www.charmedanddark.com/returns' },
  title: 'Returns & Exchanges',
  description: 'Charmed & Dark return and exchange policy. 30-day return window for eligible items.',
};

export default function ReturnsLayout({ children }) {
  return children;
}
