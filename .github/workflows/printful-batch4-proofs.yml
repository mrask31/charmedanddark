name: Obsidian Crown Moon native proofs
on:
  push:
    branches: [crew/printful-api-probe]
    paths:
      - .github/workflows/printful-batch4-proofs.yml
      - scripts/printful-batch4-proofs.mjs
permissions:
  contents: read
concurrency:
  group: printful-batch4-proofs
  cancel-in-progress: false
jobs:
  proof:
    if: github.ref == 'refs/heads/crew/printful-api-probe'
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    environment: printful-api-probe
    steps:
      - name: Retrieve fixed source at immutable run commit
        env:
          PROOF_COMMIT: ${{ github.sha }}
        run: |
          set -euo pipefail
          [[ "$PROOF_COMMIT" =~ ^[0-9a-f]{40}$ ]]
          mkdir -p scripts
          for name in printful-migration-token-preflight.mjs printful-batch4-proofs.mjs printful-batch4-plans.json; do
            curl --fail --silent --show-error --proto '=https' --tlsv1.2 --max-time 30 "https://raw.githubusercontent.com/mrask31/charmedanddark/$PROOF_COMMIT/scripts/$name" --output "scripts/$name"
          done
      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020
        with:
          node-version: '24'
      - name: Generate three native proofs within provider rate limits
        env:
          PRINTFUL_MIGRATION_API_TOKEN: ${{ secrets.PRINTFUL_MIGRATION_API_TOKEN }}
        run: node scripts/printful-batch4-proofs.mjs
