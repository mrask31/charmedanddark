name: Link Signature white tee draft fulfillment
on:
  push:
    branches: [crew/printful-api-probe]
    paths:
      - .github/workflows/printful-link-signature-white-tee.yml
      - scripts/printful-link-signature-white-tee.mjs
permissions:
  contents: read
concurrency:
  group: printful-link-signature-white-tee
  cancel-in-progress: false
jobs:
  link:
    if: github.ref == 'refs/heads/crew/printful-api-probe'
    runs-on: ubuntu-24.04
    timeout-minutes: 8
    environment: printful-api-probe
    steps:
      - name: Retrieve fixed source at immutable run commit
        env:
          LINK_COMMIT: ${{ github.sha }}
        run: |
          set -euo pipefail
          [[ "$LINK_COMMIT" =~ ^[0-9a-f]{40}$ ]]
          mkdir -p scripts
          for name in printful-migration-token-preflight.mjs printful-link-signature-white-tee.mjs printful-link-signature-white-tee.test.mjs; do
            curl --fail --silent --show-error --proto '=https' --tlsv1.2 --max-time 30 "https://raw.githubusercontent.com/mrask31/charmedanddark/$LINK_COMMIT/scripts/$name" --output "scripts/$name"
          done
      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020
        with:
          node-version: '24'
      - name: Validate exact draft and catalog gates
        run: node --test scripts/printful-link-signature-white-tee.test.mjs
      - name: Link only the 21 Signature white tee draft variants
        env:
          PRINTFUL_MIGRATION_API_TOKEN: ${{ secrets.PRINTFUL_MIGRATION_API_TOKEN }}
        run: node scripts/printful-link-signature-white-tee.mjs
